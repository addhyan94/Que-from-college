// Wap to input a string and calculate total number of special character.

#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
char a[50],ch;
int i,c,l;
printf("\n enter a string=");
gets(a);
strlen(a);
for(i=0; i<l; i++)
{
    ch=a[i];
    if()
}
}